BASE_URL=https://portainer.example.com/api

# Optional: how the webhook waits for an async redeploy to finish.
# POLL_INTERVAL_MS=5000     # how often to poll the stack status (default 5s)
# POLL_TIMEOUT_MS=1800000   # give up after this long (default 30m; 0 = no timeout)

# Optional: logging verbosity.
# LOG_LEVEL=info            # debug | info | warn | error | silent (default info)
